<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:flowable="http://flowable.org/bpmn"
             targetNamespace="http://www.flowable.org/processdef">

  <process id="reimbursement-approval" name="报销审批流程" isExecutable="true">
    
    <!-- 开始事件 - 提交报销申请 -->
    <startEvent id="start" name="提交报销申请">
      <documentation>员工提交报销申请，状态从DRAFT变为PENDING</documentation>
    </startEvent>
    
    <!-- 更新状态为PENDING -->
    <serviceTask id="updateToPending" name="更新状态为PENDING"
                 flowable:expression="${reimbursementApprovalService.updateStatus(execution.getVariable('reimbursementId'), 'PENDING')}">
      <documentation>将报销单状态更新为PENDING</documentation>
    </serviceTask>
    
    <!-- 1. DEPT_MANAGER审批 (org-roam规范第3a步) -->
    <userTask id="deptManagerApproval" name="部门经理审批" 
              flowable:candidateGroups="DEPT_MANAGER"
              flowable:formKey="deptManagerApprovalForm">
      <documentation>DEPT_MANAGER审批报销申请（org-roam规范第3a步）</documentation>
    </userTask>
    
    <!-- 部门经理审批结果网关 -->
    <exclusiveGateway id="deptApprovalGateway" name="部门经理审批结果">
      <documentation>判断部门经理审批结果</documentation>
    </exclusiveGateway>
    
    <!-- 2. ACCOUNTANT审批 (org-roam规范第3b步) -->
    <userTask id="accountantApproval" name="会计审批"
              flowable:candidateGroups="ACCOUNTANT"
              flowable:formKey="accountantApprovalForm">
      <documentation>ACCOUNTANT审批报销申请（org-roam规范第3b步）</documentation>
    </userTask>
    
    <!-- 会计审批结果网关 -->
    <exclusiveGateway id="accountantApprovalGateway" name="会计审批结果">
      <documentation>判断会计审批结果</documentation>
    </exclusiveGateway>
    
    <!-- 金额判断网关 - 如果金额超过1000，CEO审批 -->
    <exclusiveGateway id="amountGateway" name="金额判断">
      <documentation>检查报销金额是否超过1000（org-roam规范第3c步）</documentation>
    </exclusiveGateway>
    
    <!-- 3. CEO审批 (org-roam规范第3c步 - 金额超过1000) -->
    <userTask id="ceoApproval" name="CEO审批"
              flowable:candidateGroups="CEO"
              flowable:formKey="ceoApprovalForm">
      <documentation>CEO审批高金额报销申请（org-roam规范第3c步）</documentation>
    </userTask>
    
    <!-- CEO审批结果网关 -->
    <exclusiveGateway id="ceoApprovalGateway" name="CEO审批结果">
      <documentation>判断CEO审批结果</documentation>
    </exclusiveGateway>
    
    <!-- 审批通过处理 -->
    <serviceTask id="processApproval" name="处理审批通过"
                 flowable:expression="${reimbursementApprovalService.processApproval(execution)}">
      <documentation>处理审批通过，更新状态为APPROVED，通知申请人和财务</documentation>
    </serviceTask>
    
    <!-- 审批拒绝处理 -->
    <serviceTask id="processRejection" name="处理审批拒绝"
                 flowable:expression="${reimbursementApprovalService.processRejection(execution)}">
      <documentation>处理审批拒绝，更新状态为REJECTED，申请人可转回DRAFT重新申请</documentation>
    </serviceTask>
    
    <!-- 审批通过结束事件 -->
    <endEvent id="endApproved" name="审批通过">
      <documentation>报销审批流程完成，申请通过</documentation>
    </endEvent>
    
    <!-- 审批拒绝结束事件 -->
    <endEvent id="endRejected" name="审批拒绝">
      <documentation>报销审批流程完成，申请被拒绝</documentation>
    </endEvent>
    
    <!-- 流程连接 -->
    <!-- 开始 -> 更新状态为PENDING -->
    <sequenceFlow id="flow1" sourceRef="start" targetRef="updateToPending"/>
    
    <!-- 更新状态 -> 部门经理审批 -->
    <sequenceFlow id="flow2" sourceRef="updateToPending" targetRef="deptManagerApproval"/>
    
    <!-- 部门经理审批 -> 判断结果 -->
    <sequenceFlow id="flow3" sourceRef="deptManagerApproval" targetRef="deptApprovalGateway"/>
    
    <!-- 部门经理审批通过 -> 会计审批 -->
    <sequenceFlow id="flow4" sourceRef="deptApprovalGateway" targetRef="accountantApproval">
      <conditionExpression xsi:type="tFormalExpression">${deptManagerDecision == 'APPROVED'}</conditionExpression>
    </sequenceFlow>
    
    <!-- 部门经理审批拒绝 -> 处理拒绝 -->
    <sequenceFlow id="flow5" sourceRef="deptApprovalGateway" targetRef="processRejection">
      <conditionExpression xsi:type="tFormalExpression">${deptManagerDecision == 'REJECTED'}</conditionExpression>
    </sequenceFlow>
    
    <!-- 会计审批 -> 判断结果 -->
    <sequenceFlow id="flow6" sourceRef="accountantApproval" targetRef="accountantApprovalGateway"/>
    
    <!-- 会计审批拒绝 -> 处理拒绝 -->
    <sequenceFlow id="flow7" sourceRef="accountantApprovalGateway" targetRef="processRejection">
      <conditionExpression xsi:type="tFormalExpression">${accountantDecision == 'REJECTED'}</conditionExpression>
    </sequenceFlow>
    
    <!-- 会计审批通过 -> 金额判断 -->
    <sequenceFlow id="flow8" sourceRef="accountantApprovalGateway" targetRef="amountGateway">
      <conditionExpression xsi:type="tFormalExpression">${accountantDecision == 'APPROVED'}</conditionExpression>
    </sequenceFlow>
    
    <!-- 金额<=1000 -> 处理通过 -->
    <sequenceFlow id="flow9" sourceRef="amountGateway" targetRef="processApproval">
      <conditionExpression xsi:type="tFormalExpression">${reimbursementAmount &lt;= 1000}</conditionExpression>
    </sequenceFlow>
    
    <!-- 金额>1000 -> CEO审批 -->
    <sequenceFlow id="flow10" sourceRef="amountGateway" targetRef="ceoApproval">
      <conditionExpression xsi:type="tFormalExpression">${reimbursementAmount &gt; 1000}</conditionExpression>
    </sequenceFlow>
    
    <!-- CEO审批 -> 判断结果 -->
    <sequenceFlow id="flow11" sourceRef="ceoApproval" targetRef="ceoApprovalGateway"/>
    
    <!-- CEO审批通过 -> 处理通过 -->
    <sequenceFlow id="flow12" sourceRef="ceoApprovalGateway" targetRef="processApproval">
      <conditionExpression xsi:type="tFormalExpression">${ceoDecision == 'APPROVED'}</conditionExpression>
    </sequenceFlow>
    
    <!-- CEO审批拒绝 -> 处理拒绝 -->
    <sequenceFlow id="flow13" sourceRef="ceoApprovalGateway" targetRef="processRejection">
      <conditionExpression xsi:type="tFormalExpression">${ceoDecision == 'REJECTED'}</conditionExpression>
    </sequenceFlow>
    
    <!-- 处理通过 -> 结束(通过) -->
    <sequenceFlow id="flow14" sourceRef="processApproval" targetRef="endApproved"/>
    
    <!-- 处理拒绝 -> 结束(拒绝) -->
    <sequenceFlow id="flow15" sourceRef="processRejection" targetRef="endRejected"/>
    
  </process>
</definitions>